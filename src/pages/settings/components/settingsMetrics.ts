// Settings-screen layout metrics — the ONE place a settings page width lives
// (Opus review 13-09: the wide width was a literal at four sites). Wide = screens
// that lay two blocks side by side (e-mail connection + signature, matching, Koios,
// CV template); narrower screens keep their own measured width until a sweep.
export const SETTINGS_MAX_W_WIDE = 1220
