// Shared card chrome styling across koiosmodels cards (FlavorsCard, TenantOverridesCard,
// PackagesCard, RoutingCard).
export const cardStyle = { border: '1px solid var(--border)', borderRadius: 10, padding: 16, marginBottom: 14, background: 'var(--surface)' }
