/**
 * COLOR_PRESETS — the curated soft palette used everywhere colours are configured
 * (status/funnel/vacancy/pool lookups + the per-user recruiter icon). Muted mid-tones
 * that stay calm and readable in both light and dark mode. One source of truth so a
 * palette change ripples everywhere.
 */
/* eslint-disable no-restricted-syntax -- DATA: fixed swatch palette offered to tenants in the colour picker */
export const COLOR_PRESETS = [
  '#64748B', '#94A3B8', '#6E8FD6', '#8C86D9', '#A98AD1', '#C98BBA',
  '#D98A8A', '#B96B6B', '#DDA071', '#C9AC64', '#79B58E', '#5FB0AC', '#6FA8C4',
]
/* eslint-enable no-restricted-syntax */
