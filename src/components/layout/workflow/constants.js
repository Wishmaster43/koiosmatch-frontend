// Shared workflow constants.

// Filter/condition operators — used by the inline FiltersField (entity modules)
// and the edge filter panel (Router branching). Kept in one place so both match.
export const OPERATORS = ['=', '≠', '>', '<', '≥', '≤', 'bevat', 'bevat niet', 'is leeg', 'is gevuld']
